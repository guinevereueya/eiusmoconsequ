// Example usage scenario
const item = {
  bounds: { x: 10, y: 20, width: 100, height: 50 },
  // Other properties...
};

// Assigning the bounds for hit testing
const b = item.bounds; // first hit test against bounding box

// Perform hit test using b.x, b.y, b.width, b.height
// Code for hit testing against the bounding box goes here...
